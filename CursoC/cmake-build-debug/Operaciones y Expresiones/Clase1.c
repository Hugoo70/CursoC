// Operadores en C

#include <stdio.h>

int main(){
    int a,b,c;
    a=10;
    //a += 3; es igual a poner a = a + 3;
    //a -= 3; es igual a poner a = a - 3;
    //a *= 3; es igual a poner a = a * 3;
    //a /= 3; es igual a poner a = a / 3;

    printf("El valor de las variables a son = %i",a);

    return 0;
}
